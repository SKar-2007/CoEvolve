[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "coevolve-elo"
version = "0.1.0"
description = "Elo rating system for CoEvolve Sandbox"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["elo"]
package-dir = { elo = "." }